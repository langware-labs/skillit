"""Known rules store — get/set for the 'known_rules' context key."""


def get(claude_session_id: str, key: str) -> str | None:
    return None


def set(claude_session_id: str, key: str, value: str) -> str | None:
    return None
